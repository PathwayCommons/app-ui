## Getting all pathway uris

```sh
rm blacklist.txt
ls -l | wc -l // determine how many files there are in the sbgn determine
find . -name "*.xml" -size -150c -delete // delete all files less than 150 bytes (these are empty xml files with just the header)
ls -l | wc -l

```
