const chai = require('chai');
const expect = chai.expect;
// const { enrichment } = require('../../../../src/server/external-services/gprofiler');

describe('Test Enrichment Analysis Service', function () {
  this.timeout(500000);
  it('parameters: valid genes', function () {
    // TODO - do not test the live service but only local code.
    expect(true);
  });
});